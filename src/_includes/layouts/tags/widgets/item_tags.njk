<div class="container">
    {% for tag in tags %}
        {% if tag != "topics" %}
            <span><a href="/" type="button" class="btn btn-tag">{{tag | capitalize}}</a></span>
        {% endif %}
    {% endfor %}
</div>